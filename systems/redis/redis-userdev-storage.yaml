apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{system}}-userdev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi 
  storageClassName: managed-csi
