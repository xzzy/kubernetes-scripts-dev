apiVersion: v1
kind: Service
metadata:
  name: {{system}}-userdev
spec:
  selector:
    workload.user.cattle.io/workloadselector: apps.deployment-{{namespace}}-{{system}}-userdev
  ports:
    - name: http 
      protocol: TCP
      port: 8080
      targetPort: 8080
      
